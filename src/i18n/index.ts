import { pt } from './pt'
import { en } from './en'
import type { Strings } from './en'

// Change this import to switch the active locale site-wide.
const strings: Strings = pt

export { strings, en }
export type { Strings }
